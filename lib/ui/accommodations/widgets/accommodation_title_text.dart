import 'package:flutter/material.dart';

class AccommodationTitleText extends StatelessWidget {
  const AccommodationTitleText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Accommodation", style: TextStyle(fontWeight: FontWeight.bold));
  }
}
